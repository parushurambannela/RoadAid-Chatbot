# Web framework
fastapi~=0.115.0
uvicorn[standard]~=0.32.0

# MongoDB
motor~=3.6.0
pymongo~=4.9.0          # motor 3.6.x requires pymongo>=4.9,<4.10

# LangChain core — 0.3.x is required for Python 3.13
langchain~=0.3.0
langchain-core~=0.3.0
langchain-community~=0.3.0
langchain-text-splitters~=0.3.0   # split out into its own package in 0.3.x

# Google Gemini via LangChain (2.x works with langchain 0.3.x)
langchain-google-genai~=2.0.0
google-generativeai~=0.8.0

# RAG / Vector store
# langchain-chroma is now the dedicated package (replaces langchain_community.vectorstores.Chroma)
langchain-chroma~=0.1.0
chromadb>=0.5.6,<0.6.0             # langchain-chroma 0.1.x requires chromadb<0.6.0 (excludes buggy 0.5.4/0.5.5)
pypdf~=5.0.0

# Environment
python-dotenv~=1.0.0

# Data validation
pydantic[email]~=2.10.0

# File upload support
python-multipart~=0.0.18

# Utilities
httpx~=0.28.0

# Authentication
python-jose[cryptography]~=3.3.0
passlib[bcrypt]~=1.7.4
bcrypt~=4.0.1
